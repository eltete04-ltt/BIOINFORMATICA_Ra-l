---
title: "Práctica 3. Mapeo de secuencias cortas"
lang: es
date: today
execute: 
  output: true
  error: true
engine: knitr
bibliography: ../../doc/references.bib
language:
   title-block-published: "Última actualización:"
   section-title-references: "Bibliografía"
format:
   html:
      embed-resources: true
editor: 
  markdown: 
    wrap: 72
---
# A tener en cuenta

Hay varios archivos de `DRR025089` y `DRR025090` que son generados durante la práctica que los he puesto en data,
aunque deberían ir en results. Realmente he puesto todos en data, porque en el momento de hacer la práctica había algunos que tenían mucho tamaño, no me di cuenta y lo metí en github. Me dio una serie de errores y me llevó mi tiempo solucionar todo, por tanto he obtado que vayan en data para no complicarme en exceso. 

# Preparación

Usaremos los programas siguientes:

-   [fastp](https://github.com/OpenGene/fastp) [@Chen2018]. Si descargas
    el ejecutable en la carpeta actual, debes concederte el permiso de
    ejecución: `chmod u+x fastp`. Y además tendrás que invocarlo como
    `./fastp` para que Bash lo encuentre. Alternativamente, puedes
    copiarlo a una carpeta en tu `PATH`. Puedes consultar el `PATH`
    mediante `echo $PATH`.

-   [bwa](https://github.com/lh3/bwa) [@Li2009]. Puedes descargar el
    código fuente con y compilarlo siguiendo estos pasos:

    ```         
    git clone https://github.com/lh3/bwa.git
    cd bwa
    make
    ```

    Puedes copiar el ejecutable a cualquier carpeta en tu `PATH` o bien,
    invocarlo con su dirección completa cada vez que quieras ejecutarlo.

-   [samtools](https://www.htslib.org/), junto con `htslib` y `bcftools`
    [@Danecek2021]. Los tres programas pueden descargarse del mismo
    enlace, donde hay instrucciones para instalar la versión más
    reciente. Para algunos sistemas operativos existen paquetes propios,
    que pueden instalarse así: `apt install samtools`.

-   [freebayes](https://github.com/freebayes/freebayes/releases)
    [@Garrison2012].

# Objetivos

1.  Control de calidad de lecturas cortas.
2.  Mapeo de lecturas cortas a un genoma de referencia.
3.  Identificación de variantes.

# Datos

A continuación descargamos un par de archivos FASTQ de la base de datos
[ENA](https://www.ebi.ac.uk/ena/browser/home). Han sido seleccionados
porque su tamaño (comprimidos) no supera los 100 MB. Son secuencias de
la cepa Tsu4-125 de *Bradyrhizobium japonicum* (bacteria simbionte de la
planta de soja). Proceden de la [Universidad de
Tohoku](https://www.tohoku.ac.jp/en/) y forman parte de un
[proyecto](https://www.ebi.ac.uk/ena/browser/view/PRJDB3261) en el que
se secuenciaron 16 cepas más de *Bradyrhizobium*.

```{bash}
if [ ! -e ../../data/DRR025089_1.fastq.gz ]; then
   # Con la opción "-o /dev/null" nos deshacemos de los mensajes.
   wget -o /dev/null \
        ftp://ftp.sra.ebi.ac.uk/vol1/fastq/DRR025/DRR025089/DRR025089_1.fastq.gz
fi
if [ ! -e ../../data/DRR025089_2.fastq.gz ]; then
   wget -o /dev/null \
        ftp://ftp.sra.ebi.ac.uk/vol1/fastq/DRR025/DRR025089/DRR025089_2.fastq.gz
fi
```

::: {.callout-note icon="false"}
## Desafío

Una vez descargados los archivos FASTQ, échales un vistazo desde la
línea de órdenes. Puedes hacerlo con `zless`, una versión de `less`
capaz de mostrar archivos comprimidos, o bien descomprimiendo los
archvios con `gunzip`. Para no ocupar espacio de disco innecesariamente,
puedes usar `gunzip -c DRR025089_1.fastq.gz | less -S`, que envía el
contenido del archivo a la salida estándar (no a un archivo nuevo), de
donde `less -S` lo lee, sin modificar nada en la carpeta. ¿Sabrías
contar el número de líneas de cada archivo?
:::

```{bash}
gunzip -c ../../data/DRR025089_1.fastq.gz | less -S | wc -l
```

Necesitaremos también el genoma de referencia de
`Bradyrhizobium japonicum`. Usaremos el de la cepa ACCC 15027 (hay
otros).

```{bash}
# Con la opción "-o /dev/null" nos deshacemos de los mensajes,
# y con "-O referencia.fa" escogemos un nombre adecuado para el archivo.
if [ ! -e ../../data/referencia.fa ]; then
   wget -o /dev/null -O ../../data/referencia.fa \
        https://www.ebi.ac.uk/ena/browser/api/fasta/GCA_042137995.2 
fi
```

::: {.callout-note icon="false"}
## Preguntas

-   ¿Crees que deberíamos guardar los archivos descargados en
    `../../data`? Sí, tiene sentido guardar datos “crudos” descargados
    (FASTQ y referencia) en una carpeta tipo data/, para no mezclar
    código con datos y poder reutilizarlos desde distintos análisis.

-   ¿Cómo podrías guardar los mensajes de `wget` en un archivo? Para
    guardar los mensajes de wget (progreso, errores y logs) en un
    archivo, se usa la opción -o archivo.log ; esto redirige toda la
    salida informativa a ese archivo sin afectar la descarga del fichero
    principal.

-   ¿De cuántas secuencias diferentes se compone este genoma de
    referencia? En un FASTA, cada secuencia empieza con una línea de
    cabecera que comienza por \>; por tanto, el número de secuencias es
    el número de líneas que empiezan por \>.

grep -c '\^\>'referencia.fa cuenta cuantos \> hay en el archivo. En el
caso de Bradyrhizobium japonicum ACCC 15027 presente 3. Tiene un
cromosoma circular y dos plásmidos.

```{bash}
grep -c '^>' ../../data/referencia.fa 
```
:::

# Control de calidad

```{bash}
if [ ! -e fastp.html ]; then
   # Si "fastp" está en la carpeta actual y  no en el PATH, escribe "./fastp"
   # en lugar de "fastp".
   fastp -i ../../data/DRR025089_1.fastq.gz \
         -o ../../data/DRR025089_1_clean.fastq \
         -I ../../data/DRR025089_2.fastq.gz \
         -O ../../data/DRR025089_2_clean.fastq \
         --merge \
         --merged_out ../../data/DRR025089_m_clean.fastq
fi
```

::: {.callout-note icon="false"}
## Ejercicios

1.  Revisa la ayuda del programa ejecutando `fastp -h`, y asegúrate de
    entender al menos las opciones que hemos usado.
2.  Revisa el informe generado por `fastp` en el archivo `fastp.html` y
    redacta un pequeño resumen de las principales conclusiones que
    sacas.

Los datos paired-end procesados por fastp muestran alta calidad inicial
(Q20 80%, Q30 71%, GC 64%) y post-filtro excelente (91% reads retenidos:
\~234K de 823K, Q20 90%, Q30 84%, \~50M bases), con remoción principal
de baja calidad (8.5%), N excesivos (0.13%) y correcciones menores; bajo
duplicado (2.7%), pico inserto 199 bp y sin adaptadores/dímeros
significativos. Conclusión: datos limpios, equilibrados (A/T\~18%,
C/G\~32%) y listos para análisis downstream como transcriptómica
bacteriana.

3.  Descarga un segundo conjunto de archivos FASTQ de la misma especie y
    repite el análisis de calidad con ellos. Puedes ver algunas opciones
    en [este
    enlace](https://www.ebi.ac.uk/ena/browser/advanced-search?result=read_run&query=study_accession%3D%22PRJDB3261%22&fields=run_accession%2Cexperiment_title%2Cstudy_accession%2Cfastq_bytes%2Cfastq_ftp%2Cscientific_name%2Cfirst_public&excludeAccessionType=run&excludeAccessions=DRR025089).
    Es mejor no descargar archivos demasiado grandes.
:::

Descargamos los nuevos archivos, o por comando de bash o través del
enlace. En este caso, he elegido el conjunto DRR025081.

    
```{bash}
# 1. Descargar (si no existen)
if [ ! -e ../../data/DRR025090_1.fastq.gz ]; then
    wget -o /dev/null -P ../../data/ \
        ftp://ftp.sra.ebi.ac.uk/vol1/fastq/DRR025/DRR025090/DRR025090_1.fastq.gz
fi

if [ ! -e ../../data/DRR025090_1.fastq.gz ]; then
    wget -o /dev/null -P ../../data/ \
        ftp://ftp.sra.ebi.ac.uk/vol1/fastq/DRR025/DRR025090/DRR025090_2.fastq.gz
fi

# 2. Control de calidad
if [ ! -e fastp_DRR025087.html ]; then
   fastp -i ../../data/DRR025090_1.fastq.gz \
         -o ../../data/DRR025090_1_clean.fastq \
         -I ../../data/DRR025090_1.fastq.gz \
         -O ../../data/DRR025090_2_clean.fastq \
         --merge \
         --merged_out DRR025090_m_clean.fastq \
         -h fastp_DRR025090.html \
         -j fastp_DRR025090.json 
fi
#los ultimos 2 argumentos para que no se mezclen con lo que se corrió anteriormente
```

Los datos paired-end procesados por fastp muestran buena calidad inicial
(Q20 78%, Q30 68%, GC 64%) y post-filtro excelente (85% reads retenidos:
\~82K de 424K, Q20 93%, Q30 88%, \~17M bases), con remoción principal de
baja calidad (15%), N excesivos (0.14%) y correcciones menores; bajo
duplicado (2.1%), pico inserto 128 bp y adaptadores mínimos
(pre-trimmados). Conclusión: Datos limpios, equilibrados (A/T\~18%,
C/G\~32%) y listos para análisis downstream como transcriptómica
bacteriana.

# Mapeo

El objetivo de esta sección es alinear las lecturas cortas al genoma de
referencia, mediante el programa `bwa`. El archivo final será un archivo
BAM *ordenado*. Para obtener ese archivo es necesario generar algunos
archivos auxiliares: primero, el índice del genoma de referencia,
después los alineamientos en formato SAM (no comprimidos) y finalmente
un archivo BAM, que es la versión comprimida del SAM (todavía no
ordenado). La jerarquía de condiciones de ejecución siguiente permitirá
borrar los archivos auxiliares, para ahorrar espacio, y no tener que
generarlos de nuevo cada vez que compilemos este documento si el archivo
final ya está presente en la carpeta de trabajo.

```{bash}
if [ ! -e ../../data/DRR025089.bam ]; then
   if [ ! -e DRR025089_unsorted.bam ]; then
      if [ ! -e ../../data/DRR025089.sam ]; then
         if [ ! -e referencia.fa.bwt ]; then
            bwa index referencia.fa
         fi
         bwa mem -M -R '@RG\tID:DRR025089\tSM:SAMD00022947' \
                 -o DRR025089.sam \
                 ../../data/referencia.fa \
                 ../../data/DRR025089_1_clean.fastq \
                 ../../data/DRR025089_2_clean.fastq \
         1> bwa.log 2> bwa.err
      fi
      samtools view -b -o DRR025089_unsorted.bam ../../data/DRR025089.sam
    fi
    samtools sort -o ../../data/DRR025089.bam DRR025089_unsorted.bam
    samtools index ../../data/DRR025089.bam
fi
```


::: {.callout-note icon="false"}
## Desafíos

1.  Entre las órdenes anteriores, añade la descarga del genoma de
    referencia en el lugar adecuado y bajo la condición adecuada para
    que pueda borrarse una vez se haya generado el alineamiento.

```{bash}
if [ ! -e ../../data/DRR025089.bam ]; then

    # Descargar referencia solo si no existe
    if [ ! -e ../../data/referencia.fa ]; then
        wget -q -O ../../data/referencia.fa \
        https://www.ebi.ac.uk/ena/browser/api/fasta/GCA_042137995.2
    fi

    # Indexar referencia si hace falta
    if [ ! -e referencia.fa.bwt ]; then
        bwa index referencia.fa
    fi

    # Alineamiento
    if [ ! -e ../../data/DRR025089_unsorted.bam ]; then
        bwa mem -M -R '@RG\tID:DRR025089\tSM:SAMD00022947' \
        ../../data/referencia.fa \
        ../../data/DRR025089_1_clean.fastq \
        ../../data/DRR025089_2_clean.fastq \
        > DRR025089.sam 2> bwa.err

        samtools view -b -o ../../data/DRR025089_unsorted.bam DRR025089.sam
    fi

    # Ordenar e indexar
    samtools sort -o ../../data/DRR025089.bam ../../data/DRR025089_unsorted.bam
    samtools index ../../data/DRR025089.bam

    # Borrar intermedios y referencia (opcional)
    rm -f DRR025089.sam ../../data/DRR025089_unsorted.bam
    rm -f ../../data/referencia.fa referencia.fa.*

fi
```

2.  En un bloque de órdenes nuevo, alinea también los pares de lecturas
    cortas que `fastp` había combinado (`DR025089_m_clean.fastq`).

```{bash}
if [ ! -e ../../data/DRR025089_merged.bam ]; then

bwa mem -M \
-R '@RG\tID:DRR025089\tSM:SAMD00022947' \
../../data/referencia.fa \
../../data/DRR025089_m_clean.fastq \
> ../../data/DRR025089_merged.sam

samtools view -b -o ../../data/DRR025089_merged_unsorted.bam ../../data/DRR025089_merged.sam
samtools sort -o ../../data/DRR025089_merged.bam ../../data/DRR025089_merged_unsorted.bam
samtools index ../../data/DRR025089_merged.bam

fi
```

3.  Lee la lista de funciones que te ofrece `samtools` al ejecutarlo sin
    ningún argumento. ¿Qué función usarías para combinar dos archvivos
    BAM en uno?

La función es samtools merge: samtools merge combinado.bam archivo1.bam
archivo2.bam

4.  ¿Cuánto espacio de disco ocupan los archivos FASTQ, el genoma de
    referencia y su índice y el archivo SAM?

```{bash}
du -h ../../data/DRR025089_1_clean.fastq
du -h ../../data/DRR025089_2_clean.fastq
du -h ../../data/referencia.fa
du -h DRR025089.sam
```

Unidades en Mb: 81, 81, 9.7 y 192, respectivamente. 5. Podíamos haber
ahorrado algún paso aprovechando las *tuberías* de BASH:

```{bash}
bwa mem -M -R '@RG\tID:DRR025089\tSM:SAMD00022947' \
        referencia.fa DRR025089_1_clean.fastq DRR025089_2_clean.fastq | \
samtools view -u - | \
samtools sort - > ../../data/DRR025089.bam
```

Este estilo tiene también la ventaja de ser más rápido. ¿Crees que hay
algún inconveniente al usar las tuberías?

El uso de tuberías en BASH permite ejecutar los programas de forma más
rápida y ahorrar espacio en disco, ya que no se generan archivos
intermedios como el SAM. Sin embargo, también tiene algunos
inconvenientes: al no guardarse los resultados intermedios, es más
difícil revisar los pasos del proceso o detectar errores. Además, si el
pipeline se interrumpe por algún fallo del sistema o falta de recursos,
es necesario repetir todo el proceso desde el principio, ya que no se
puede continuar desde un paso intermedio.
:::

Antes de proseguir, es importante comprobar el resultado del paso
anterior. Podemos extraer algunas estadísticas del archivo BAM así:

```{bash}
if [ ! -e DRR025089.stats.txt ]; then
   samtools stats ../../data/DRR025089.bam > DRR025089.stats.txt
fi

# Extraemos la sección sobre profundidad de cobertura
if [ ! -e stat.$i.txt ]; then
   grep ^COV DRR025089.stats.txt | cut -f 2- > COV.txt
fi
```

Ahora podemos importar los datos de cobertura a una sesión de R y
representarlos gráficamente:

```{r}
COV <- read.table('COV.txt',
                  col.names = c('intervalo', 'cobertura', 'bases'))
plot(COV$cobertura, COV$bases, type = 'l')
```

::: {.callout-note icon="false"}
## Desafíos

1.  La distribución de cobertura tiene una cola larga. ¿Puedes centrar
    la representación gráfica entre 0 y 20? Pista: usa la opción `xlim`
    de `plot()`.

```{r}
COV <- read.table('COV.txt',
                  col.names = c('intervalo', 'cobertura', 'bases'))
plot(COV$cobertura, COV$bases, type = 'l', xlim=c(0,20))
```

2.  ¿Qué otras estadísticas crees que vale la pena revisar?

```{r}
mean(COV$cobertura)
median(COV$cobertura)
sd(COV$cobertura)
quantile(COV$cobertura)
```

3.  El genoma de referencia incluye dos plásmidos. ¿La cepa secuenciada
    los tiene? Cuando un genoma tiene plásmidos, normalmente aparecen
    picos adicionales en la distribución de cobertura, porque los
    plásmidos suelen tener un número de copias distinto al cromosoma
    (muchas veces mayor), lo que genera coberturas diferentes.

Como en la gráfica solo aparece un pico principal y no hay otros picos
claros, lo más probable es que la cepa secuenciada no contenga los dos
plásmidos del genoma de referencia, o que su presencia sea muy baja y no
genere un pico detectable en la distribución de cobertura.

4.  Visualiza los alineamientos en el terminal con la orden siguiente:

```{bash}
#| eval: false
# No lo renderizo porque tarda mucho
samtools tview ../../data/DRR025089.bam ../../data/referencia.fa
```
:::

# Identificación de variantes

```{bash}
if [ ! -e ../../data/DRR025089.vcf ]; then
   freebayes -p 1 -f ../../data/referencia.fa ../../data/DRR025089.bam > ../../data/DRR025089.vcf
fi
```
